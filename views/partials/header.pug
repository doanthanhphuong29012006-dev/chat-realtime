header.chat-header
  .logo
    a(href="/")
      img.header-logo(src="/images/rat.svg", alt="Logo Chat Chit")
      span.logo-text
        i.fa-solid.fa-house
        | Trang chủ

  .user-profile
    a(href=`/user/edit`)
      .avatar-mini
        img(
          src=user.avatar
          alt=user.fullName
        )
        
    span.username #{user.fullName}
    a(href="/auth/logout")
      button.btn-logout Đăng xuất