header.chat-header
  .logo
    a(href="/users/suggestions")
      img.header-logo(src="/images/rat.svg", alt="Logo Chat Chit")

  .user-profile
    a(href=`/user/edit`)
      .avatar-mini
        img(
          src=user.avatar
          alt=user.fullName
        )
        
    span.username #{user.fullName}
    a(href="/auth/logout")
      button.btn-logout Đăng xuất